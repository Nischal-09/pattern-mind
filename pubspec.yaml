name: pattern_mind
description: A new Flutter project called PatternMind.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.4.1
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  animate_do: ^3.3.4
  gap: ^3.0.1
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
